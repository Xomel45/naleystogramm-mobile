<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ── Сеть ──────────────────────────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- UPnP discovery (multicast) -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- ── Аудио: голосовые звонки + голосовые сообщения ─────────────────────── -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- ── Foreground Service (API 26+) ─────────────────────────────────────── -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- API 34+: обязателен если сервис использует микрофон -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- API 34+: для передачи файлов в фоне -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- ── Уведомления ───────────────────────────────────────────────────────── -->
    <!-- API 33+: runtime-разрешение для показа уведомлений -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- ── Файловый обмен ───────────────────────────────────────────────────── -->
    <!-- API 33+: scoped media permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- API 26–32: fallback до scoped storage -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- ── Системные ─────────────────────────────────────────────────────────── -->
    <!-- Удерживать CPU активным во время звонков и передачи файлов -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Автозапуск MessagingService после перезагрузки -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- KeyProtector: биометрическая аутентификация через Android Keystore -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:name=".NaleystogrammApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Naleystogramm">

        <!-- Главный экран; singleTop = одиночный экземпляр (аналог QLocalServer) -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Экран входящего/исходящего звонка -->
        <activity
            android:name=".ui.CallActivity"
            android:exported="false"
            android:launchMode="singleTask" />

        <!-- Фоновый сервис: держит TCP-соединения, принимает сообщения и звонки -->
        <service
            android:name=".service.MessagingService"
            android:exported="false"
            android:foregroundServiceType="microphone|dataSync" />

    </application>

</manifest>
