package com.xomel45.naleystogramm.ui

import androidx.appcompat.app.AppCompatActivity

class MainActivity : AppCompatActivity() {
    // TODO: entry point, hosts NavHostFragment
    // single-instance equivalent: handle intent with FLAG_ACTIVITY_SINGLE_TOP
    // Mirrors: main.cpp entry + MainWindow
}
