package com.xomel45.naleystogramm.core

class NetworkManager {
    // TODO: TCP client/server, JSON frames with length-prefix (same protocol as desktop)
    // PING/PONG keepalive, exponential backoff reconnect, relay mode, rate limiting
    // Mirrors: src/core/network.h — port 47821, kMinPeerVersion 0.7.2
}
